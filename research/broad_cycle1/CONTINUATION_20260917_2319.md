# Broad Discovery Cycle-1 continuation checkpoint — 2026-09-17 23:19 CST

Governance: PRE-PERFORMANCE. No Cycle-1 candidate outcome is authorized/read by this checkpoint. Reserved BTC holdouts and GC transport remain untouched. Validated Alpha = NO.

## Durable execution findings

The project branch `broad-cycle1-data-20260917` contains the frozen Cycle-1 runner and two data-freeze workflows. The latest push commit `ad1b8e8ffd51f96d5d2e9533046e87eef8b49647` passed the repository CI matrix, but both research workflows failed before producing an artifact.

1. `broad-discovery-cycle1-data` run 35226512050 failed at the data acquisition step because the GitHub-hosted runner received HTTP 451 from `https://fapi.binance.com/fapi/v1/klines`. This is an execution-region/geofencing boundary, not an Alpha result.
2. `broad-cycle1-data` run 35226512122 failed before performance because Binance Vision has no monthly `BTCUSDT-1d-2019-09.zip` at the requested path (HTTP 404). No result artifact exists.
3. The older hardened downloader already contains a monthly->daily fallback, but the later REST replacement removed that fallback and hit 451. Therefore the next safe engineering action is to restore the archive-based fallback and explicitly handle pre-archive launch days without changing any strategy/performance rule.

## Exact next task

- Keep the performance embargo in force.
- Determine the earliest official Binance Vision USD-M BTCUSDT daily archive date. If official archives do not cover 2019-09-08..first-archive-date-1, use the already-connected Binance historical query only for that missing prefix, persist those exact rows into a frozen repository data seed with provenance/hash, and use Binance Vision only for the archive-covered suffix. Do not fabricate or proxy missing rows.
- Freeze normalized dataset + manifest + SHA256 and verify 2019-09-08..2026-09-15 continuity.
- Only after the data gate passes, freeze the exact preregistered runner/code hash and execute the single consumed-development read; retain every candidate outcome and do not touch reserved holdouts.
- GC Candidate-A remains independently frozen pending durable exact COMEX payload access.
